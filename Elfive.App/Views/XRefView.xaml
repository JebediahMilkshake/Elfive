<UserControl x:Class="Elfive.App.Views.XRefView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <!-- Banner -->
        <Border DockPanel.Dock="Top"
                Padding="10,6"
                Background="{x:Static SystemColors.ControlBrush}"
                BorderBrush="{x:Static SystemColors.ControlDarkBrush}"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Label Content="Context:" VerticalAlignment="Center" Padding="0,0,4,0"/>
                <ComboBox ItemsSource="{Binding XRefContextOptions}"
                          SelectedItem="{Binding XRefContext}"
                          Width="200"
                          VerticalAlignment="Center"
                          Margin="0,0,16,0"/>
                <Label Content="Tag:" VerticalAlignment="Center" Padding="0,0,4,0"/>
                <TextBox Text="{Binding XRefTagInput, UpdateSourceTrigger=PropertyChanged}"
                         Width="280"
                         Padding="4,2"
                         VerticalAlignment="Center"
                         VerticalContentAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Results -->
        <DataGrid x:Name="ResultsGrid"
                  ItemsSource="{Binding XRefResults}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  MouseDoubleClick="ResultsGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tag Name"    Binding="{Binding TagName}"        Width="180"/>
                <DataGridTextColumn Header="Instruction" Binding="{Binding InstructionName}" Width="100"/>
                <DataGridTextColumn Header="Routine"     Binding="{Binding Routine}"         Width="160"/>
                <DataGridTextColumn Header="Program"     Binding="{Binding Program}"         Width="160"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}"     Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
